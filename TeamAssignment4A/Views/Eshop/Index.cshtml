@model IEnumerable<TeamAssignment4A.Models.JointTables.CandidateExam>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="BuyExam">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CandidateExamId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CandidateExamId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CandidateExamId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
